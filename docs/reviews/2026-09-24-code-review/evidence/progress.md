# SDD ledger — plan: docs/superpowers/plans/2026-09-24-code-review-final.md

Plan: 34 tasks (0–33). Baseline commit c2d2aee (engine-identical: c2d2aee..HEAD touches only docs/superpowers/plans/).
Baseline suite: MEASURED 666 passed, exit=0 (2026-09-25, log: cache/scratch/baseline-suite.log). Non-root host (uid 1000): Task 2 unreadable-lock test RUNS here, so non-root counts are the gate (667 → 806).
Stage-only: no commits anywhere. Implementers `git add` their task's files and stop. Review packages built from `git diff -U10 HEAD -- <files>` (working-tree vs HEAD).
Constraint deltas carried to every dispatch:
- Repo root /opt/projects/kanban/main/kanban; no worktrees.
- export PYTHONDONTWRITEBYTECODE=1 for anything that touches a mutation proof; single-file runs: /usr/bin/python3 -m pytest -q tests/<file>.py; whole suite: PYTHON=/usr/bin/python3 ./test.sh.
- Never commit; never stage docs/superpowers/plans/; never touch boards/**/work|runs, TIMELINE.md, boards/*/README.md.
- If a hunk does not apply: re-read the site, apply by hand, report the drift (a finding about THIS task only if a test fails differently).
- A test failing for a DIFFERENT reason than the brief's "measured red state" is a finding — report, do not bend the test.

Pre-flight scan (2026-09-25):
- Self-consistency: every diff hunk's file is in its task's Files list (all 34 tasks) — clean.
- Shared files (25): driver/run.py (3,5,6,8,9,10,13,14,15,17,20,21,24,28,29,30,31,32,33), driver/run-audit.py (2,3,4,17,28,30,31,32), driver/file_lanes.py (15,16,29,30,31,32), template/board_schema.py (11,12,19,33), template/lanes.py (11,28,30,31,33), driver/create-board.sh (1,8,29), driver/render-flow.py (14,22,29,32), driver/doc-chain.py (4,32), driver/runs_util.py (17,30,33), driver/timing-report.py (17,25,30), driver/start-board.sh (18,28,31), tests/test_acquire_lock.py (2,5,18), tests/test_board_schema.py (11,12,19,33), tests/test_card_stops.py (20,21), tests/test_chain_log.py (0,24,30,31,33), tests/test_driver_main.py (5,9), tests/test_lanes_graph.py (11,14,33), tests/test_manifest_shape.py (8,9,10,13,29), tests/test_open_lane.py (0,13,17,24), tests/test_rework_loop.py (17,21,29,33), tests/test_run_audit.py (2,3,4,7,17,26,31), tests/test_run_directories.py (3,6,17,30), tests/test_runs_util.py (17,33), tests/test_suite_hygiene.py (0,32), tests/test_unstarted_mint.py (1,15,29).
  All by design: the plan's patches apply sequentially onto the tree the previous task left. No conflict expected; a hunk that fails to apply is drift to report, not a task failure.
- Harness deltas (sdd-in-hermes-harness): delegate_task has no model field — all children inherit the session model (no tiering); children cannot ask — "stage and ask" becomes "stage and report"; operator (controller) verifies cheap decisive claims before accepting.

Gates (whole-suite, non-root): T0=667 T1=669 T2=677 T3=684 T4=695 T5=713 T6=714 T7=716 T8=721 T9=724 T10=726 T11=741 T12=748 T13=750 T14=752 T15=755 T16=759 T17=765 T18=766 T19=767 T20=769 T21=771 T22=773 T23=776 T24=777 T25=780 T26=785 T27=790 T28=790 T29=794 T30=794 T31=799 T32=800 T33=806.
Mutation proofs (strictly alone): T5 (run.py halt-order), T7 (run-audit.py --json stats key), T26.

Task 0: complete (stage-only, files: tests/test_chain_log.py tests/test_open_lane.py tests/test_refinement_option.py tests/test_suite_hygiene.py; single-file 88 passed; suite 667 = gate; review Approved; package task-0-review-package.diff 130 lines).
Task 0: minor (deferred): hygiene test substring path_ready match (plan-mandated; contrived).
Task 0: minor (deferred): hygiene test dies with raw SyntaxError on a broken test file (plan-mandated; still loud).
Task 0: Ruling (review Important, plan-mandated): ast.ImportFrom/importlib blind spot in test_suite_hygiene.py — kept the plan's byte-identical code; the plan was replayed end-to-end on exactly this code (806 passed), and the per-file single-file gate still enforces the real contract for ImportFrom files; cost if wrong: a future `from card_render import …` test file slips the hygiene test but dies loudly at its own single-file run. Carried to final review for triage.

Task 1: complete (stage-only, files: driver/create-board.sh tests/test_unstarted_mint.py; red: 2 failed/26 passed matching brief; single-file 28 passed; suite 669 = gate; review Approved, no Critical/Important; package task-1-review-package.diff 96 lines).
Task 1: minor (deferred): open(CREATE).read() without encoding= (plan-mandated verbatim; gate host UTF-8 default; not actionable under gate).

Task 2: complete (stage-only, files: driver/run-audit.py template/driver_lock.py tests/test_acquire_lock.py tests/test_run_audit.py; red: 5 failed/60 passed — brief's 4 reds + the non-root-only unopenable-lock test, all for the brief's reasons; single-file 65 passed; suite 677, 0 skipped = non-root gate; review Approved, no Critical/Important; package task-2-review-package.diff 504 lines). Note: brief's red-state list assumed root (skips that test); on this host the 5th red is expected, not drift.
Task 2: minor (deferred): pid_alive lacks OverflowError catch (pre-existing, outside brief's patch; audit side already catches it) — one-line hardening candidate for a later task (Tasks 30/31 sweep swallowed failures).
Task 2: minor (deferred): alive-branch audit wording prints empty pid when file unreadable (plan-mandated wording; dead branch has the 'unreadable' fallback, alive branch doesn't).
Task 2: minor (deferred): _driver_alive maps PermissionError-on-open to (False, None) = 'none' not 'unreadable' (plan-mandated corner; 'unreadable' pinned only for empty file).
Task 2: minor (deferred): second take() in one process overwrites _held_fd, leaking the first fd (production takes exactly once at run.py main(); no live-lock hole; known corner for future multi-board-in-process callers).

Task 3: complete (stage-only, files: driver/run-audit.py driver/run.py tests/test_run_audit.py tests/test_run_directories.py; red: 6 failed/80 passed matching brief (E4 missing, 3 JSONDecodeError, AttributeError on list manifest, RuntimeError not raised), pin passed; single-file 86 passed; suite 684 = gate; review Approved, no Critical/Important; package task-3-review-package.diff 436 lines; all 4 post-image blob hashes match the brief's headers).
Task 3: minor (deferred): .tmp file left behind on a kill mid-write (inert: every reader reads the final path only; overwritten next write).
Task 3: minor (deferred): no test for a run-summary.json parsing to a non-object (board.json half covered; same _load_json path).
Task 3: minor (deferred): no CLI-level test for a missing board.json (covered at audit() level; main()'s ceiling=None branch).
Task 3: minor (deferred): os.replace without fsync (plan-mandated; power-loss window remains, kill-mid-write fixed).
Task 3: observation (pre-existing, NOT this task): driver/run-audit.py is mode 644 in the index/worktree (verified: git ls-files -s = 100644) while README.md:367 / AGENTS.md:26 / TIMELINE.md:15 invoke it directly as an executable; the brief's patch header claimed 100755 — moot because the diff changes no mode. Carry to final review: either the doc invocations are aspirational or the repo wants a chmod +x commit (operator's call; no task in this plan owns it).

Task 4: complete (stage-only, files: driver/doc-chain.py driver/run-audit.py tests/test_doc_chain.py tests/test_run_audit.py; red: 9 failed/74 passed matching brief (6 KeyError params, 2 JSONDecodeError, 1 AttributeError-load_report ordering artifact) + 2 pins green; single-file 83 passed; suite 695 = gate; review Approved, no Critical/Important; package task-4-review-package.diff 706 lines).
Task 4: minor (deferred): analyze() still raises on wrong-TYPED fields (non-ISO ts -> ValueError; inputs null/list -> AttributeError in the row loop's sorted(r.get("inputs", {}).items()) while producers guards it with `or {}`; unresolved null -> TypeError) — brief explicitly scoped the fix to MISSING keys; real residual hazard, no plan task owns it.
Task 4: minor (deferred): --history exits 1 on a torn chain without printing the torn count (finding appended before the history branch, which prints only history()) — plan-mandated placement.
Task 4: minor (deferred): docstring "a record that lacks what a check needs is not judged" overstates (title/inputs-missing records ARE judged with defaults) — plan-mandated wording.
Task 4: minor (deferred): no all-garbage-chain test through audit()/main (only through doc-chain's main) — same code branch, low risk.
Task 4: observation: brief's red-state note listed JSONDecodeError for the torn-line test; first failure is AttributeError (load_report absent) since the test touches the new API before appending — ordering artifact, not a defect.

Task 5: complete (stage-only, files: driver/run.py tests/test_acquire_lock.py tests/test_driver_main.py; red: 12 failed/22 passed = brief's 12 (all AttributeError timeout_seconds) — my dispatch's extra-2 prediction was wrong, brief authoritative; single-file 34 passed; suite 713 = gate; MUTATION PROOF bit exactly test_a_halt_raised_inside_the_tick_exits_one_without_finishing ('finish_run ran on a halted run'); restore proven twice (sha256 60a76669... identical pre/post and re-measured by controller; pycache cleared; suite green after); review Approved, no Critical/Important; package task-5-review-package.diff 495 lines).
Task 5: minor (deferred): --timeout-min 1e307 accepted (finite, positive) but minutes*60 overflows to inf → cap that never fires; docstring claims that class is prevented. Fix would be checking math.isfinite(cap) after the multiply.
Task 5: minor (deferred): the flag scan does not skip the token it consumed, so `--timeout-min --timeout-min=5` reads 300.0 instead of erroring (pathological argv only).
Task 5: minor (deferred): test-side global leak — main() sets write_summary._t0 and the test_driver_main fixture does not restore it; latent order dependence, harmless with today's test set.
Task 5: minor (deferred): the positive half of the halt-order invariant is unasserted (both finished-path tests stub finish_run to a no-op), so deleting the finish_run() call would keep the new file green.
Task 5: dispatch note (mine): the brief's Step 4b `git diff --stat driver/run.py must be this task's diff only` is unsatisfiable pre-staging under a stage-only plan; the implementer proved the restore equivalently (sha256 before/after + unstaged diff byte-identical to the brief's hunk). Controller confirmed the hash and the green suite.

Task 6: complete (stage-only, files: driver/run.py tests/test_run_directories.py; red: 2 failed/30 passed matching brief (nothing copied; nothing logged); single-file 32 passed; suite 714 = gate; review Approved; package task-6-review-package.diff 341 lines; both post-image blobs match brief). Test REPLACED as mandated (inspect.getsource grep -> behaviour test).
Task 6: Ruling (review Important, plan-mandated): the new "no provenance patches found under ..." line repeats once per tick (preserve_artifacts is re-entered from _gate_action's gc/PASS branch every tick until the gate card is done; the pre-existing "artifact kept:" line is naturally deduped by the exists() guard, the new line has none). Kept the plan's byte-identical patch — the plan's measured replay is the evidence chain and a local guard would break byte-exactness; impact is log noise only (goes to stdout + driver.log), no data loss, no state corruption, tests unaffected. cost if wrong: noisy driver.log on a board with auto-gates off and a human-held gate; carried to the final review as a plan-level dedup follow-up.
Task 6: minor (deferred): no test pins the ABSENCE of the empty-case line when a patch exists (first test sets log to a no-op).

Task 7: complete (stage-only, file: tests/test_run_audit.py; test-only coverage task: no red, both new tests green on the unmodified engine; MUTATION PROOF bit test_the_json_contract_is_what_the_caller_reads (AssertionError ['findings','rows'] == ['findings','rows','stats']); restore proven twice (sha256 77bbb40f... identical pre/post and re-measured by controller; worktree-vs-index diff empty; pycache cleared; suite green); single-file 58 passed; suite 716 = gate; review Approved, no Critical/Important; package task-7-review-package.diff 168 lines).
Task 7: minor (deferred): rows/stats VALUES unpinned ({"findings": [...], "rows": [], "stats": null} would pass) — brief pins the three key names only.
Task 7: minor (deferred): the findings comparison re-runs ra.audit(runs) rather than an independent expectation (correct for a contract test; same code path).

Task 8: complete (stage-only, files: driver/create-board.sh driver/run.py tests/test_manifest_shape.py; red: 3 failed/2 passed matching brief; single-file 33 passed; suite 721 = gate; review Approved, no Critical/Important; package task-8-review-package.diff 401 lines; blobs match brief 6114b30/c904a15/9f8bc57; R3 honoured: fallback {"slug": BOARD} fresh literal, no shared mutable defaults; reviewer confirmed no consumer depended on the four dropped keys).
Task 8: minor (deferred): the heredoc test is a source-text scan (cannot catch a malformed heredoc or a consumer default flipping) — plan-mandated; reviewer verified the emission by direct probe.
Task 8: minor (deferred): unused import pytest in tests/test_manifest_shape.py (carried from the brief).
Task 8: minor (deferred): fallback slug uses env BOARD, not basename(BOARD_DIR) (coincide for every real board; differs only for BOARD="").

Task 9: complete (stage-only, files: driver/run.py tests/test_driver_main.py tests/test_manifest_shape.py; red: 2 failed + 5 fixture-ERRORs, one reason (no attribute require_manifest_valid) matching brief; single-file 24 passed; suite 724 = gate; review Approved; package task-9-review-package.diff 570 lines; gate provably before acquire_lock and before any DB write/mint).
Task 9: Ruling (review Important — outside the brief's letter, real gap in the whole change): an UNPARSEABLE board.json still dies with an uncaught JSONDecodeError at the import-time read run.py:226 (WORKDIR = manifest().get(...)); card_render.read_board is a bare json.load, so the new startup gate (which runs inside main()) never sees it. Plan's Review Focus #2 asks that a truncated board.json never traceback — Task 3 fixed the AUDIT path (E4), no task in this plan owns the driver's import-time read. Ruling: keep Task 9's plan-mandated letter as-is and carry this to the FINAL whole-change review as a candidate for the fix pass (it is a named-behaviour gap, not a transcription defect). cost if wrong: a board with a torn board.json halts the driver with a traceback instead of a named halt.
Task 9: minor (deferred): refusal message hardcodes "board.json" instead of passing where=BOARD_CFG (repo already passes where in tests/test_shipped_boards.py) — brief-mandated text.
Task 9: minor (deferred): the shipped-boards loop has no per-slug label, contradicting tests/test_shipped_boards.py:32-34's stated convention — brief-owned.
Task 9: minor (deferred): the ordering test stubs both sides, so it pins order only (the real gate is covered by test_manifest_shape.py).
Task 9: note: other entry points (driver/file_lanes.py:125, :234) also read the manifest raw, ungated.

Task 10: complete (stage-only, files: driver/run.py tests/test_manifest_shape.py; red: 2 failed (AttributeError IdeaFileBlank / last_lane_with_idea) matching brief; single-file 9 passed; suite 726 = gate; review Approved; package task-10-review-package.diff 566 lines; blobs match brief 10ccbe6/d41fe4e).
Task 10: Ruling (review Important — contrived state, out of brief): write_summary reads EVERY lane (run.py:3413) and finish_run wraps it in a broad except Exception (run.py:3310), so a blank idea in a lane past the scan's break raises IdeaFileBlank, is logged as "WARNING: summary generation failed (non-fatal)", and the run then logs ALL GATES COMPLETE with NO run-summary.json — the E4 false-positive shape Task 3 documented. Reachable only with the lane already in STATE.opened plus a lower lane's file absent (two hand edits in that order). Ruling: keep the brief's letter; carry to the FINAL review as a candidate fix-pass item (same class as Task 9's import-time read: a will-not-parse/emptied file reaching an unguarded reader). cost if wrong: a missing summary on a contrived-state run, reported honestly by the auditor as E4 but with a misleading "no summary" wording.
Task 10: minor (deferred): the six+ other lane_options sites propagate IdeaFileBlank out of tick() to main's broad handler → traceback logged, then a named halt after 3 identical ticks (delayed, not silent — reviewer verified the halt reason names the exception and file).
Task 10: minor (deferred): lane_options stats the path a second time after read_idea's own stat (run.py:358 vs template/lanes.py:483) — a create/truncate in that window raises a spurious, sticky false halt (contrived).
Task 10: minor (deferred): lane_model_opts still calls lanes.read_idea directly (run.py:378), so a blank file reads as "no model header" there (residual conflation, defaults).

Task 11: complete (stage-only, files: template/board_schema.py template/lanes.py tests/test_board_schema.py tests/test_lanes_graph.py; red: 8 failed/84 passed matching brief (0s/0m/0h0m, relative target, per-lane providers + one model, 1h30m wrongly rejected, bare-string gates, bare-string goal args); single-file 92 passed; suite 741 = gate; --check-schema current + render-flow --check exit 0 also observed; review Approved, no Critical/Important; package task-11-review-package.diff 293 lines; R5/R6/R9 verified not-widened; parse single-sourced).
Task 11: minor (deferred): the generated schema's duration/paths PATTERNS are still the old lenient form until Task 12 (deliberate — Task 12's line item).
Task 11: minor (deferred): bare "~" accept branch untested; ~user/x refused although expanduser would honour it; the new provider/model elif also fires phrased-for-per-lane on a provider_override list (cosmetic); no accept-side pin for 0h30m/0.5h.

Task 12: complete (stage-only, files: template/board.schema.json template/board_schema.py tests/test_board_schema.py; red: 3 failed/80 passed matching brief (lanes required, both-sides refusal, unwritable target traceback); single-file 83 passed; suite 748 = gate; --check-schema current, regeneration byte-identical (sha256 84d402f1); review Approved, no Critical/Important; package task-12-review-package.diff 708 lines; reviewer independently reproduced the generated JSON via --jsonschema | diff = empty, so no hand-edited generated file).
Task 12: minor (deferred): residual counterexample — {"slug":"b","$schema":1} is validate-ACCEPTED (validate skips $-keys) but schema-REFUSED (properties.$schema typed + additionalProperties false); 213-case sweep found this as the only one; one-line fix on either side.
Task 12: minor (deferred): the agreement test is a 13-case spot check, not an enumeration of OPTIONS (unlike test_every_option_kind_has_a_schema_entry).
Task 12: minor (deferred): the 0m collapse rule is declared only in Python; the $comment names the lanes-length rule only (report overclaimed).

Task 13: complete (stage-only, files: driver/run.py tests/test_manifest_shape.py tests/test_open_lane.py; red: 2 failed/55 passed matching brief (AttributeError card_model_args; AssertionError mispaired provider in open_lane); single-file 57 passed; suite 750 = gate; review Approved, no Critical/Important; package task-13-review-package.diff 858 lines; run.py + test_manifest_shape.py blobs byte-match the brief; all six lane-scoped call sites routed through the new builder, no remaining path can pair a lane model with the board provider; no test rewritten (patch purely additive)).
Task 13: minor (deferred): a lane header naming only a model now DROPS the board's provider instead of inheriting it — plan-mandated and pinned by the new test (lanes.py itself warns a bare -m resolves against the profile's provider); follow-up decision, not a deviation.
Task 13: minor (deferred): a manifest-level per-lane model ARRAY (the R6 shape) reaches _model_pair verbatim (['--model', ['m1','m2']]) — pre-existing, reachable only via file_lanes.py:198/revision filing, no shipped board uses it.
Task 13: minor (deferred): card_model_args re-reads manifest() per card where open_lane already holds board_cfg (brief-mandated body).

Task 14: complete (stage-only, files: driver/render-flow.py driver/run.py tests/test_lanes_graph.py; red: 1 failed (AttributeError UnknownGateKind) + vocabulary pin, matching brief; single-file 18 passed; suite 752 = gate; render-flow.py --check exit 0; review Approved; package task-14-review-package.diff 797 lines; derivation provably behaviour-preserving; UnknownGateKind names the offending kind + vocabulary and is not swallowed).
Task 14: Ruling (review Important, plan-mandated line): driver/render-flow.py:39 reaches the vocabulary via the module-attribute hop lanes.board_schema.GATE_CODES instead of importing board_schema directly (run-audit.py does it directly). Kept the plan's byte-identical line: Tasks 22/29/32 patch render-flow.py with context measured against it, and the plan's end-state verification is defined on this exact content; the failure mode is loud anyway (any test loading render-flow raises AttributeError at import). cost if wrong: a future refactor of lanes.py's imports breaks render-flow at import time (loudly, with the existing render-flow tests as witness); carried to the final review as a one-line style fix the operator can take post-merge.
Task 14: minor (deferred): render-flow's half of "one source" is not test-pinned (reverting :39 to a literal renders identically and passes).
Task 14: minor (deferred): a fifth gate code now CRASHES render-flow on FILL[code]/SHORT[code] (traceback instead of a message) — loud, an improvement over the previous silent mismatch.
Task 14: minor (deferred): _tick filters unknown kinds, so the new raise is reachable only through the module API/tests, not a real tick path.
Task 14: minor (deferred): residual gate literal at run.py:300 (c["code"] in ("RVa","Gc")) — pre-existing, outside the patch.

Task 15: complete (stage-only, files: driver/file_lanes.py driver/run.py tests/test_unstarted_mint.py; red: 3 failed/28 passed matching brief (AttributeError RUN_ID_RE, DID NOT RAISE, assert '../../x' is None); single-file 31 passed; suite 755 = gate; review Approved; package task-15-review-package.diff 948 lines; all three blobs byte-match the brief; R8 honoured (no shape guard on the readers — RUN_ID_RE appears nowhere in run.py); containment is a single-segment check (strictly equivalent to containment under RUNS_ROOT, cannot be defeated by ../runs-fake); missing vs escaping distinguished; all 70 dirs under boards/*/runs/ still accepted).
Task 15: minor (deferred): the producer shape pin at file_lanes.py:123 is an assert → inert under python -O (spec-mandated, disclosed).
Task 15: IMPORTANT (residual, out of the brief's hunks — carried to the final review): not every reader of runs/current goes through the new check — file_lanes.unstarted_mint:79-85 joins it with only an isdir guard and its return flows into next_run_key's REUSE path (:119-121), which bypasses the shape assert, so create-board.sh:473-484 can makedirs outside runs/ and write current there given a hand-edited pointer to an existing dir (e.g. "../../../<slug>/work"); same for driver/timing-report.py:72-74. One-line close available (is_safe_run_name is in the same module); bounded by the isdir precondition.

Task 16 (Filing refuses instead of guessing; I10/I13/I14/I31): complete (stage-only, files: driver/file_lanes.py tests/test_file_lanes.py; red: 2 failed/23 passed — one red surfaced one step later (FileNotFoundError from render_body) than the brief's predicted ValueError; reviewer PROBED and confirmed SAME root cause (the blanket except swallowed the malformed manifest's JSONDecodeError, so control fell into the missing-manifest branch) — not a second defect, test left unmodified (correct); single-file 25 passed; suite 759 = brief's gate; review Approved; package 279 lines).
Task 16: Important (brief-level, inherited): both red tests assert only the failure TYPE (pytest.raises(ValueError), no match=) — an unrelated ValueError would satisfy them; for a task about discriminating failure reasons a message assertion would be strictly better. Carried to the final review.
Task 16: minor (deferred): the remaining blanket except Exception inside src() at file_lanes.py:288 can still drop a CONFLICTS line silently (near-dead in practice).
Task 16: observation (carried to the final review): the task FRAMING ("the card-body pipeline goes through one entry point") is not what the brief/diff does — no entry point was added; render_body still has two callers (file_lanes.py:207 filing, run.py:714 rework) and read_board has four differently-policied call sites. Consolidation was not in the brief's scope.
Task 16: minor (deferred): the real CLI path refuses earlier and uncaught (create-board.sh:477 calls file_lanes._board_cfg with no try) — a raw traceback on the real path; the narrowing is defense-in-depth for file_board's own callers.

Task 17 (A refused runs CLI is UNKNOWN, not an empty card; I15): complete (stage-only, files: driver/run-audit.py driver/run.py driver/runs_util.py driver/timing-report.py + tests/test_open_lane.py test_rework_loop.py test_run_audit.py test_run_directories.py test_runs_util.py; red: 6 failed/202 passed matching brief (TypeError NoneType not iterable at run.py:639/3421, missing E6 unknown finding, assert [] is None); task tests 208 passed; suite 765 = brief's gate; review Approved, no Critical/Important; package 1899 lines; UNKNOWN is a distinct type at the boundary (None vs []), all five BOARD call sites updated, E6 fires ungated so the audit exits 1, no in-tree consumer mis-reads None).
Task 17: REWRITE verified legitimate: tests/test_runs_util.py::test_board_runs_warns_instead_of_reporting_an_empty_card — HEAD asserted board_runs(...) == [] + warning; now is None + warning kept (strictly stronger), REWRITTEN 2026-09-24 docstring present, and the [] -is-data side pinned by a new companion test.
Task 17: minor (deferred): driver/timing-report.py:113 still does board_runs(...) or [] so a refused CLI prints as absent minutes there (I15 half-closed for that consumer; T25 is meant to close it).
Task 17: minor (deferred): the refusal warning names board/card/stderr but not the CLI command or exit status.
Task 17: minor (deferred): driver/run.py:1464-1465 record_timing skips the transition on None and writes no UNKNOWN marker into the cache — if that card never transitions again its enrichment is silently lost.
Task 17: minor (deferred): the exit-0-with-unreadable-stdout path is exercised by no test (nonzero-exit refusal is covered).

Task 18 (The cron door asks the question the other doors ask; I16): complete (stage-only, files: driver/start-board.sh tests/test_acquire_lock.py; red: 1 failed/18 passed on the new SOURCE assertion (assert 'live_driver_pid "$REPO/boards/$SLUG"' in src) exactly as the brief predicted; task tests 19 passed; suite 766 = brief's gate; review Approved, no Critical/Important; package 316 lines; the cron door now asks the identical question as create-board.sh via the shared live_driver_pid helper (same function, same argument semantics — absolute board dir), and the check demonstrably gates BOTH launch paths (one-shot :109 and serve :116), with the racing-tick backstop at template/driver_lock.py:68-71).
Task 18: minor (deferred): the refusal exits 0 (brief-mandated, matches the file's documented idempotent-cron contract) so cron/monitoring cannot tell "already running" from "started"; it names board+pid but suggests no remedy for a wedged driver (create-board's refusal exits 6 with a remedy).
Task 18: minor (deferred): the guard sits after mkdir -p of runs/ (:82), so an idle-board cron tick still creates runs/ when a live driver is found.
Task 18: minor (deferred): LOCK= (:81) is now assigned and never read (shellcheck SC2034; no CI consequence).
Task 18: minor (deferred): the positive assertion is a whole-file substring, so placement (call above the launch) is not pinned — plan-wide pattern.

Task 19 (A failing index read is not a clean index; I17): complete (stage-only, files: template/board_schema.py tests/test_board_schema.py; red: 1 failed/83 passed (AssertionError: [] on notices == []) matching brief; task tests 84 passed; suite 767 = brief's gate; --check-schema current, exit 0; review Approved, no Critical/Important; package 554 lines; notice names where/repo/exit code/git stderr + "unknown, not clean"; truncation bounds only the quoted cause so the exit code and verdict cannot be lost; a genuinely absent/clean index still returns [] (pinned)).
Task 19: minor (deferred): the notice goes to stdout with exit 0, so an exit-code-only caller (board_schema.py board.json >/dev/null) sees nothing — the report-not-refuse contract boundary, not a defect against the task.
Task 19: minor (deferred): report-only inaccuracies (claims strip() flattens multi-line stderr — it only trims ends, so a multi-line git error yields a broken multi-line notice; and an old-side hunk-header number) — no code change needed.

FLAKE OBSERVED (controller, 2026-09-25, during Task 20 verification; NOT a Task 20 change): one full-suite run failed at tests/test_acquire_lock.py::test_an_older_driver_without_the_flock_is_still_refused (1 failed, 768 passed). Mechanism (read from source): that test spawns a child (subprocess.Popen([sys.executable, fake_run_py, "--serve"])) and immediately reads the lock; template/driver_lock.py:114-118 reads /proc/<pid>/cmdline and returns True only when some argv basename == run.py, so in the fork/execve window argv is empty and the lock is taken over instead of refused -> SystemExit not raised -> "DID NOT RAISE". Unreproducible under no load: 5/5 passes in isolation, then 769 passed x3 on the whole suite (incl. the re-verify). Test file is Task 2's patch (Task 18 added a test to the same file, did not touch this one); no later task in the plan touches tests/test_acquire_lock.py.
FLAKE: consequence for the end-state claim — the plan's own completion criterion ("806 passed") is not reliably reproducible while this fixture race exists; measure the end state with a retry and report both numbers if it fires. Carried to the final review with this evidence (candidate: a readiness wait in the fixture, e.g. poll /proc/<pid>/cmdline until it names run.py, which is a deviation from the plan's byte-exact test and therefore deferred rather than applied here).

Task 20 (The halt counter keys on the error's shape; board removal matches one phrase; I18/I19): complete (stage-only, files: driver/run.py tests/test_card_stops.py; red: 2 failed/85 passed matching brief (AssertionError None — varying ids reset the counter; assert None == 0 — case-sensitive literal missed both other wordings); task tests 87 passed; suite 769 = brief's gate (verified twice after the flake, see FLAKE entry); review Approved, no Critical/Important; package 1138 lines; both named risks probed LIVE by the reviewer: varying-id messages group across ticks (ValueError: card t_# unreadable after # s) while different failures stay apart, and 7 realistic lock/permission/unreadable wordings naming the board do NOT fire the removal branch (only the two real wordings do; the b10-vs-b1 slug trap still covered)).
Task 20: minor (deferred): digit masking is coarse by design and can merge failures distinguished only by a number (exit code 1 vs 137); separation is pinned only via a TYPE change, so no test catches an over-aggressive future masker.
Task 20: minor (deferred): ids outside \bt_\w+ (bare hashes/UUIDs, run ids) still reset the count every tick.
Task 20: minor (deferred): board-removal is still CLI-text parsing — a third wording falls to the generic branch and keeps driving (inherent until the CLI exposes a structured error).

Task 21 (An unreadable card escalates instead of stalling; I19 unreadable half + errors I14 surviving half): complete (stage-only, files: driver/run.py tests/test_card_stops.py tests/test_rework_loop.py; suite 771 = the brief's gate; package 1437 lines).
Task 21 PROVENANCE DEVIATION: the dispatched implementer (deleg_09973953 / sa-0-82f85030) applied STEP 1 ONLY and then reported completed with its final message cut mid-sentence; it wrote no report and did not run Steps 2-5. The controller completed Steps 2-5 mechanically from the brief: red measured (2 failed/147 passed, both AttributeError 'RunState' has no attribute 'tick_serial' at test_card_stops.py:1283/:1294 = the feature absent, matching the brief's "both red"); the brief's Step-3 fence (brief lines 92-173) extracted and diffed IDENTICAL against the child's scratch patch before git apply --check clean; all three blobs equal the brief's stated targets (run.py 014cc5d, test_card_stops c4f8837, test_rework_loop 2c7c0f4); task tests 149 passed; staged 3 files (30 entries, no new paths).
Task 21: named check for the reviewer — the Step-1 patch modifies TWO stubs in tests/test_rework_loop.py (45+/2- vs HEAD) with an explanatory comment but NO "REWRITTEN 2026-09-24" marker (0 occurrences in both test files); ruling needed: superseded contract (marker required) vs accidental-fixture fix.
Task 21: process note — child aborted after Step 1 without a report; controller completed the mechanical steps. If this recurs, consider adding "do not return until Step 5 is done and the report is written" to the dispatch context.

Task 21: review Approved, no Critical/Important; package 1437 lines. Reviewer verified base->worktree deltas contain EXACTLY the brief's hunks for all three files (bases genuinely post-Tasks-0-20), red-for-the-right-reason structurally confirmed (base d5cfb9e has neither tick_serial nor count_unreadable; both new tests touch run.STATE.tick_serial). Named risk (a) clean: only two call sites count, both via count_unreadable which compares unreadable_counted[cid] against STATE.tick_serial[0] and increments at most once per serial; tick_serial advances only in tick(), whose sole caller is the main loop. Named risk (b) clean: escalate always calls record_halt unconditionally -> halt_if_exhausted returns it -> _tick_preflight True -> serve loop exits (not increment-and-never-read).
Task 21 REWRITE RULING (no marker required): the two test_rework_loop stubs changed from a catch-all `kb -> ""` to `"{}" if a[:1] == ("show",)`; the old stub declared no show semantics and its "" was not JSON, so card_record read it as a failed read; no assertion pinned that side effect (the three consumers assert only halt + block --kind needs_input), the change was FORCED by the new unreadable branch (run.py:2826 continues before the exhaustion check), and it makes the fixture honest. Contrast Task 17, which changed an ASSERTION (== [] -> is None) and therefore carries the marker.
Task 21: minor (deferred): the once-per-tick test calls count_unreadable twice directly rather than driving the two real scans in one tick — pinned at counter-unit level, not end-to-end.
Task 21: minor (deferred): the escalation message names the card by gate/code only (code = title.split(":")[0]); the id is in escalate's ledger/comment but not in the reason text.

Task 22 (`--check` names a missing README; `--check` is tested failing; I20/S9, tests S1): complete (stage-only, files: driver/render-flow.py tests/test_render_flow.py; red: 1 failed/3 passed (FileNotFoundError from render-flow.py:168 readme = open(README).read()) matching the brief; green 4 passed; suite 773 = the brief's gate; render-flow --check exit 0; package 190 lines).
Task 22 DRIFT REPAIRED BY CONTROLLER: the implementer appended an extra "REWRITTEN 2026-09-24 (plan test-rewrite list): ..." block INSIDE the rewritten test's docstring, which already carried the brief's own "REPLACED 2026-09-24 — ..." text — extra lines not present in the brief's patch (the deviation was invited by my own dispatch constraint wording, which said REWRITTEN; the plan uses BOTH words, REPLACED in this brief and at tests/test_run_directories.py:413, REWRITTEN at tests/test_acquire_lock.py:34 and tests/test_runs_util.py:58). The controller removed the added block, restoring the brief's exact docstring. Both blobs now equal the brief's stated targets: tests/test_render_flow.py 80df37b, driver/render-flow.py 851cc8b; task tests re-run 4 passed; suite re-verified 773; re-staged (31 files).
Task 22: process note — do NOT impose a marker WORD in dispatch constraints; the brief's own text governs (the marker word is not asserted by any test).

Task 22: review Approved, no Critical/Important; reviewer confirmed BOTH blobs byte-equal the brief's stated targets (render-flow.py 851cc8b, test_render_flow.py 80df37b; test pre-image 080bb8c = the brief's pre-image exactly) and that no invented marker remains. Named risk (a) reproduced first-hand in a scratch copy: stale flow.mmd -> rc 1 + "stale: driver/flow.mmd"; README missing -> rc 1 + "stale: README.md (missing — nothing to splice the diagram into)"; the non-check write path with a missing README -> SystemExit naming the abs path. Named risk (b) clean: the stale test mutates only the tmp copy, pins the copy current first (assert rf.main() == 0) and excludes the missing-file shortcut (assert "README" not in out). Rewrite strictly stronger (6 words x 2 files vs 1 word x 1 file, diagnostic message, brief's marker).
Task 22: minor (deferred): only FileNotFoundError is caught at render-flow.py:172 — a README that is a directory or unreadable still tracebacks (IsADirectoryError/PermissionError); brief-verbatim.
Task 22: minor (deferred): encoding asymmetry — the initial README read uses encoding="utf-8" (:169) but the stale re-read at :179 uses the locale default.
Task 22: minor (deferred): task-22-report.md is now STALE — its "deliberate deviation" section still describes the removed REWRITTEN sentence; the ledger entry "Task 22 DRIFT REPAIRED BY CONTROLLER" and the reviewer's finding supersede it.

Task 23 (`arm.sh` runs for real; its title fallback is reachable; I21, comments PRIOR-I3): complete (stage-only, files: driver/arm.sh tests/test_arm_script.py (NEW); red: 2 failed/1 passed matching brief (headingless run: returncode=1 stdout='' stderr='' — set -e aborted before the Idea $LANE fallback; guard test: 'caught downstream' present); green 3 passed; suite 776 = the brief's gate; package 156 lines; review Approved, no Critical/Important; reviewer reproduced the red independently in a scratch copy and verified end to end that the fallback title reaches hermes create (argv log shows `create Idea 1 --body ... --initial-status blocked`), that the replacement comment is factually TRUE (validate_armed has no duplicate-lane check; armed_ideas/adopt_and_refile write lane-N.md once per entry, last wins), and that the stub env leaks nothing (no new untracked files, no board/run/lock; stub first on PATH)).
Task 23: minor (deferred): the third test's positive half ("last" in src and "Arm a lane once" in src) pins incidental wording; the discriminating half is the negative assertion, so a re-added claim in different words would slip through.
Task 23: minor (deferred): the stub always answers list with [] so the seeded-Triage archive loop (arm.sh:60-69) is never exercised by any test.
Task 23: minor (deferred): `|| true` at arm.sh:40 also swallows a genuine grep/sed failure (verified acceptable: an unreadable idea still aborts before create, so the fallback is reached only for a headingless-but-readable idea).
Task 23: minor (deferred): unescaped TITLE reaching hermes create for headings containing backslash/percent (pre-existing).

Task 24 (The ledger creates the directory it writes into; I22): complete (stage-only, files: driver/run.py tests/test_chain_log.py tests/test_open_lane.py; red: 1 failed/76 passed (FileNotFoundError opening the run-dir verdicts.jsonl) matching brief; green 77 passed; suite 777 = the brief's gate; package 1458 lines; review Approved, no Critical/Important; the created directory is the file's OWN parent (os.path.dirname(STATE.verdicts_path)) not a hardcoded board dir; run.py:1864 is the ONLY append to verdicts.jsonl and every ledger writer (757, 2132, 2440, 2597, 3018) funnels through ledger(); makedirs does not swallow EACCES — PermissionError is caught by the same except OSError and logged with the path; the new test asserts the verdict SURVIVES (reads it back, checks PASS, and that no log line was produced), not merely that nothing raised; the implementer's flagged Pyright dirname(None) concern is unreachable (use_run() runs at module scope, run.py:220, so verdicts_path is always a str)).
Task 24: minor (deferred, candidate follow-up): a run-dir disappearance mid-tick can now be masked — _tick() checks run_directory_is_gone() at run.py:2356, but a ledger() write later in the same tick recreates the run dir, so the next tick sees an existing directory and halt_run_directory_gone() never fires; the same property already belonged to chain_record/card_log (run.py:900, 1523) so it is a pre-existing pattern the brief prescribed.
Task 24: minor (deferred): missing vs unwritable directory are distinguished only by embedded errno text in one message (run.py:1867); no test covers the unwritable branch.

Task 25 (`timing-report` parses argv in main() and says when minutes are UNKNOWN; I25 + I15 report half): complete (stage-only, files: driver/timing-report.py tests/test_timing_report.py (NEW); red: 3 failed/3 collected — all three via SystemExit: unknown arg: -q raised from the import-time _args(sys.argv[1:]) at timing-report.py:81, i.e. the brief's "3 red at import"; green 3 passed; suite 780 = the brief's gate; package 268 lines; review Approved; both blobs reproduced independently from the brief's own patch (784818f / 6c62ea1); named risk (a) clean — AST dump shows no argv work at module scope and fresh-interpreter imports under ['-q',...], ['-k','timing'], ['--jsonl'], [], ['-h'], ['--board','b'] all rc=0; as __main__ -q -> rc=1 unknown arg, bare -> rc=1 --board required, --help -> rc=0; named risk (b) clean — a refusing CLI prints the UNKNOWN banner naming the card codes and stating the totals exclude them, with the concrete error on stderr via runs_util._warn_once).
Task 25: IMPORTANT (spec-prescribed residual, CANDIDATE FOR THE FINAL REVIEW FIX PASS, with reproductions): `rows = runs_elapsed(cid) or []` at timing-report.py:257 leaves a MIXED-refusal hole. Probe A (CLI always refuses): banner present, totals exclude — thesis met. Probe B (call 1 ok, call 2 refused): no banner, no BUDGET line, a silently lost flag. Probe C (call 1 refused, call 2 ok): banner says the card is left out while BUDGET still prints. Fix: `if rows is None: continue` or reuse the first pass's rows.
Task 25: minor (deferred): main() returns 0 with UNKNOWN cards (brief-prescribed; the test pins == 0) so a scripted gate cannot detect an incomplete total by exit status.
Task 25: minor (deferred): the banner names card CODES (title prefix, :189) not ids, and len(unknown) can double-count a code across titled rounds; the banner's reason is generic (concrete error only on stderr).

Task 26 (The auditor's uncovered outcomes gain tests; I28/I29/I30, tests S6): complete (TEST-ONLY, stage-only, files: tests/test_run_audit.py (+51); suite 785 = the brief's gate; package 271 lines; review Approved, no Critical/Important; file byte-exact to the brief — the reviewer reconstructed the pre-image (deleting :209-212 and :831-877) to 826 lines sha256 43feaff3 = the report's "before", showing Task 26 added exactly 51 lines and no hand edits hid elsewhere).
Task 26 MUTATION PROOF: credible. Mutation `if cards and not agent:` -> `and False:` at driver/run-audit.py:232 makes the SOLE E10 branch unreachable -> findings [] -> test_cards_with_no_agent_minutes_are_an_e10 fails with the reported AssertionError: [] / assert 'E10' in [] at :865. Controller independently confirmed the revert: grep 'and False' = 0, run-audit.py worktree == index == blob 63fbb75 (Task 17's) and worktree sha256 c28287ed = the report's pre-mutation hash. Reviewer also confirmed each new test bites its own branch (E10 sole site :233; E4 no-gate-evidence :194-195; Gi :203-206; external-workdir guard :347-348; /proc parse :363-372), that E10 pins code AND severity via codes(findings, "WARNING"), and that the I28 /proc test is host-robust (own-pid read + a pid above PID_MAX_LIMIT, so no real foreign process needed).
Task 26: minor (deferred): the Gi E4 assertion (:856) does not pin severity (engine emits ERROR at run-audit.py:204) — brief-verbatim.
Task 26: minor (deferred): the I28 test hard-fails on a host without /proc (macOS) rather than skipping — acceptable since the audited code itself hard-depends on /proc/<pid>/status.

Task 27 (`runs-report`'s uncovered paths and its stat loop; I36, errors S9): complete (stage-only, files: driver/runs-report.py tests/test_runs_report.py; red: 1 failed/14 passed — only the vanished-directory test red, the rest pins, exactly as the brief says (FileNotFoundError from the unguarded getmtime); green 15 passed; suite 790 = the brief's gate; package 231 lines; review Approved, no Critical/Important; reviewer applied the brief's fences to pristine HEAD copies and got byte-identical results (staged blobs 9bbb48f / f56ae6b = the brief's post-images) and reproduced the red independently; the guard sits at the RIGHT level (the whole per-run row body at runs-report.py:108, not just the stat) and catches ONLY FileNotFoundError (:119) so PermissionError still surfaces; a vanished run is named on stderr before the table).
Task 27: minor (deferred, candidate for the final review): the `--json` path carries NO record of a vanished run — the entry is dropped with no vanished key/count, so a machine consumer reading stdout sees a silently under-counted list (the only notice is stderr). Same class as Task 17's UNKNOWN theme.
Task 27: minor (deferred): os.listdir(runs) (:103), the flat-layout branch (:122) and flat_leftovers (:133) sit outside the try, so the report still dies if the whole runs/ tree vanishes; PermissionError/NotADirectoryError are never caught.
Task 27: minor (deferred): the printed '<N> run(s)' and totals silently exclude vanished runs (note says "not listed" but does not flag the total as short).

Task 28 (The engine's prose matches its behaviour; I37-I39/I42/S5/S7/S8 + PRIOR-I5/I8/R1/R7/R9/n1/n5 + types T-23): complete (stage-only, files: driver/run-audit.py driver/run.py driver/start-board.sh template/card-bodies/{_result-field,gc-body,rvp-body}.txt template/lanes.py; NO red step by the brief's own design (prose only, receipts are greps); task-relevant files 268 passed; suite 790 = the brief's gate; render-flow --check exit 0; package 1999 lines; review Approved, no Critical; patch identity PROVEN by blob equality on all 7 files (no extra edit possible); only executable change is the E10 message at run-audit.py:233-235 which now names the field truly read (agent_union_min under the same is-not-None test)); every sampled claim verified true (latest_verdict_card fallback + (card, None) on refusal, gc count/8-path log truncation, rvp <PLAN> path, retired 'stale outputs cleared' in BOTH start-board.sh:100-102 and open_lanes, the lane-field comments vs their readers).
Task 28: IMPORTANT (plan-mandated false-ish claim, CANDIDATE FOR THE FINAL REVIEW — exactly the pattern this task exists to remove): driver/run.py:2358-2360 says the mint rule "is argued in tests/test_run_directories.py and enforced by run-audit's E16". E16 is an INFO note about litter under work/ (run-audit.py:334-362) that by its own code cannot fail a run (run-audit.py:579) and never inspects a run directory. Suggested fix: "...argued in tests/test_run_directories.py; run-audit's E16 notes the litter the board leaves instead of deleting it".
Task 28: minor (deferred, Task 30 candidate): clean_work_noise's docstring (run.py:2752) cites test_nothing_in_the_template_deletes_work_or_runs, which does not exist (the real test is test_the_driver_retired_every_clearing_function, tests/test_run_directories.py:158) — pre-existing in HEAD, untouched by these hunks; Task 30 removes dead code and should retire the function.
Task 28: minor (deferred): run-audit.py:246-247 shows "-" as the in-flight row's result example, but an in-flight row's result is "" (doc-chain.py:205); the "-" placeholder belongs to inputs/attachments.
Task 28: minor (deferred): gc-body.txt's "the driver records how many files are staged in the result" only holds on an auto-gate board (run.py:1433-1434); on a human board the count sits in the driver's GATE READY comment/summary gate text.
Task 28: minor (deferred): run.py:871-875 "taken when the gate opens" is re-taken every tick the gate is held (:2492).

Task 29 (create-board.sh: the help, the gate before creation, one pointer writer; I41/I43/I44/S3 + errors I11/S7/S8 + code S7/S8/S20 + comments PRIOR-R6/R8/n2): complete (stage-only, files: driver/create-board.sh driver/file_lanes.py driver/flow.drawio driver/render-flow.py driver/run.py tests/test_manifest_shape.py tests/test_rework_loop.py tests/test_unstarted_mint.py; red: 4 failed/101 passed, each for its own intended reason (quote-in-title JSONDecodeError; slug-manifest gate ValueError substring not found; registry that cannot be read assert 0 == 4; runs-current one writer AttributeError set_current_run); task tests 105 passed; suite 794 = the brief's gate MEASURED WITH HERMES_HOME/GIT_DIR STRIPPED; render-flow --check exit 0 (the hand-applied flow.drawio equals generator output); package 2631 lines; no test rewritten).
Task 29 ENV FINDING (controller-verified both ways): with this session's ambient HERMES_HOME=/home/wos/.hermes/profiles/coder, the suite gives 1 failed/793 passed at tests/test_tool_clis.py::test_an_existing_profile_the_cli_cannot_list_is_a_note_not_a_refusal; with `env -u HERMES_HOME` that file is 6 passed and the suite is 794 passed. Cause: Task 29's new HERMES_ROOT="${HERMES_HOME:-$HOME/.hermes}" (create-board.sh:325) is consulted by the profile pre-flight (:330-337), while test_tool_clis._stub_env overrides HOME but NOT HERMES_HOME (tests/test_tool_clis.py:94-99) — so from inside a Hermes session the fake-home fixture is no longer isolated. Not a defect in the 8 files' content (all match the brief; test_tool_clis.py is outside this task's file list). CARRIED TO THE FINAL REVIEW, same class as the acquire_lock flake: either _stub_env should clear HERMES_HOME, or the pre-flight root should not assume HERMES_HOME is the Hermes ROOT when in this host's layout it is a PROFILE dir. Real-world impact low — with the real `hermes` CLI an existing profile is named by `hermes profile list`, so the branch yields a note rather than a refusal.
Tooling: verify-task.sh now runs the suite under `env -u HERMES_HOME -u GIT_DIR` (canonical gate); the two plain-env runs above were the ones that exposed this.

Task 29: review Approved, no Critical/Important; package 2631 lines. Reviewer verified by content hash: 7 of 8 staged blobs byte-identical to the brief's post-images (create-board.sh efb5769, file_lanes.py 745a808, flow.drawio bc5bc81, render-flow.py ff9f112, run.py 1e6ac89, test_manifest_shape.py 6266a82, test_rework_loop.py 71a34b3). Named risk (a) clean: file_lanes.set_current_run (file_lanes.py:112-123) is the only implementation, with exactly two call sites (create-board.sh:512, run.py:204); no other runs/current write anywhere in driver/, template/ or any .sh. Named risk (b) clean: the gate chain (manifest :287 -> profile :327 -> skills :363 -> goal :419 -> dispatcher :436 -> driver-pid :452 -> registry :463) precedes every creation side effect (boards create :473, mkdir WORKDIR :476, mkdir BOARD_DIR :479, board.json :480, run dir + pointer :511-512). Escaping verified live as json.dumps (quote/unicode/backslash/leading-dash titles all produce parseable JSON). Registry read exits 4 on failure (distinct from empty). Help claims each checked against the schema/boards they name.
Task 29 DRIFT REPAIRED BY CONTROLLER: tests/test_unstarted_mint.py was staged with only 2 backslashes in the new docstring where the brief has 4 (blob da7e148 vs the brief's 8239e33) — docstring-only, but it lost the escape the sentence describes. Controller restored the brief's exact text; the file now hashes to the brief's target 8239e33, tests 35 passed, suite re-verified 794, re-staged.
Task 29: minor (deferred): create-board.sh:83 claims "Every board.json carries "$schema": ..." while the manifest this script writes (:283) has no $schema (all 7 shipped boards do; board_schema treats it as optional) — the one help sentence still not true of the script's own output.
Task 29: minor (deferred): the build (:282) and the write (:477) are gated by the same condition written twice; if a later edit separates them an empty board.json is written silently — no test pins the equality.

Task 30 (Dead code, hidden imports, hidden state; S2/S4 + prior S1/S2/S6/S12/S13/S16/S21/S23/S24 + types T-17/T-18 + code S4 + comments PRIOR-R2/R3/R4 + errors S12): complete (stage-only, files: driver/file_lanes.py driver/run-audit.py driver/run.py driver/runs_util.py driver/timing-report.py template/lanes.py tests/test_chain_log.py tests/test_run_directories.py; red: 2 failed/61 passed = AttributeError 't0' (test_chain_log.py:306) and 'timing_started' (test_run_directories.py:121), both expected; task tests 63 passed; suite 794 = the brief's gate (run with HERMES_HOME/GIT_DIR stripped); render-flow --check exit 0; package 3058 lines; review Approved, no Critical/Important).
Task 30: reviewer recovered the EXACT task delta from the object DB (git diff <pre-blob> <post-blob>) and found all 36 hunks present with nothing else; all 8 working files hash to the brief's post-images. Deletions proven unreferenced repo-wide (parse_elapsed_minutes, record_timing._started, write_summary._t0, file-local subprocess in timing-report; opts/retries/lane_options gone from _gate_action). Test rewrites NOT weakened (single code line each; the t0 injection still cannot silently stop working because test_chain_log.py:320 asserts overhead within 0.2). The semantic change is value-equivalent minus the mutation, and the mutation had NO reader (no code reads state[...] last_run/gave_up; the timing cache and timing.jsonl consumers are unaffected) — T-17 justified.
Task 30: minor (deferred, CANDIDATE FOR THE FINAL REVIEW — prose left false BY this patch): template/lanes.py:377-379 still says MAX_REWORKS "lives here rather than with the function above because this module's board_schema imports come after the graph (a module-level read up there is a NameError)" — this patch moves the imports to lanes.py:17-21, making it false; no hunk covers it, so it was left verbatim.
Task 30: minor (deferred): driver/run.py:2756 (clean_work_noise tombstone) still cites the nonexistent test_nothing_in_the_template_deletes_work_or_runs (real: test_the_driver_retired_every_clearing_function, tests/test_run_directories.py:158) — Task 28 deferred it to Task 30 but no hunk fixed it; the new comment at run.py:2743-2744 points readers at that docstring.
Task 30: minor (deferred): timing-report.py:96-115 transitions()/card_ids() are two identical passes over the same snaps.
Task 30: minor (deferred): the new comment at run.py:1487-1488 says the old full.update(c) "was then discarded by the re-merge"; it actually persisted in state[t] and the re-merge made it redundant.

Task 31 (The remaining swallowed failures and unchecked statuses; S12 + errors S1-S4/S7/S10-S12 + code S9/S10/S14): complete (stage-only, files: driver/file_lanes.py driver/reset.sh driver/review-package.sh driver/run-audit.py driver/run.py driver/start-board.sh template/lanes.py tests/test_chain_log.py tests/test_run_audit.py tests/test_tool_clis.py; red: 4 failed/101 passed matching the brief verbatim (silent except OSError: pass; cards=[] on a failed list; unchecked git diff --cached status; lowercase-only y) with the review-package help test as a pin; no test rewritten; task tests 105 passed; suite 799 = the brief's gate (HERMES_HOME/GIT_DIR stripped); render-flow --check exit 0; package 3412 lines; review Approved, no Critical/Important).
Task 31: reviewer verified the CENTRAL check in both directions with real execution for five sites (log() normal writes with no NOTICE vs one NOTICE naming the path; repo_findings clean/staged -> no finding vs rc 128 -> E14 with git's stderr; board_findings readable -> nothing vs rc 1 -> E12 "database is locked"; unstage_run_paths clean -> silent vs one WARNING naming REPO+stderr; _options_line conflict/mismatch tests still pass). Exit-code semantics safe (uses `diff --cached --name-only`, which exits 0 WITH a staged diff, never --quiet). reset.sh accepts y/Y/yes/YES/Yes and rejects n/no/''/maybe/YEs/yES/true/1 (exit 1). The single narrowed catch (run.py:1527 -> except (RuntimeError, OSError)) covers exactly what kb can raise there (TimeoutExpired/refusal -> RuntimeError, missing CLI -> FileNotFoundError ⊂ OSError), with card_log's broad catch at run.py:1544 still logging anything else; the 14-left-broad decision is defensible (all commented; all but card_record log inline, and card_record sets STATE.read_error which escalates via count_unreadable/halt_if_exhausted).
Task 31: minor (deferred, CANDIDATE FOR THE FINAL REVIEW): driver/run.py:2249's new WARNING has NO once-per-condition guard (unlike the task's own log()/STATE.log_write_failed and the house pattern) — a sustained index failure emits one line per tick, and each line then trips the auditor as ('ERROR','E2','log line: WARNING: cannot read the index …') (measured).
Task 31: minor (deferred): start-board.sh:71-76 dropping 2>/dev/null lets a raw JSONDecodeError traceback print beside the clean message (largely unreachable since validate_board_files runs at :65).
Task 31: minor (deferred): reset.sh:155 swaps xargs -r -d '\n' -> xargs -0 (drops -r; xargs -0 runs once on empty input) so the [ -n "$staged" ] guard is now load-bearing — unreachable from git diff --cached --name-only output, latent fragility.
Task 31: minor (deferred): test_reset_accepts_an_uppercase_yes pins only exit 0 + "no driver running", not that n/'' are still refused (reviewer verified by hand); the restore --staged refusal and the start-board exit 2 have no test.

Task 32 (Every file the engine opens is closed by a `with`; S3 + code S3): complete (stage-only, files: driver/doc-chain.py driver/file_lanes.py driver/render-flow.py driver/run-audit.py driver/run.py tests/test_suite_hygiene.py; red: 1 failed/1 passed — the new hygiene test listed EXACTLY six bare-open sites (doc-chain.py:221, file_lanes.py:321, render-flow.py:182, run-audit.py:278, run.py:1390, run.py:1384); task tests 2 passed; suite 800 = the brief's gate; render-flow --check exit 0; package 3145 lines; review Approved, no Critical/Important).
Task 32: reviewer re-derived the red independently (reverting only those six conversions reproduces exactly the brief's six sites, so the mapping is 1:1) and ran an independent AST scan: 0 bare open() sites remain in template/ + driver/ (50 open() calls, all in with). Mode byte-identical at all six; NO lifetime grew (run-audit.py:278 and doc-chain.py:221 SHORTEN it — the old run-audit form leaked on a mid-read raise, doc-chain's handle died with the loop iterator); nothing holds a closed file object (all consumers are str/list[str]). The hygiene scan genuinely catches a regression (an injected bare open was reported as driver/runs_util.py:200 with file:line) and does not false-positive on a string containing open( or on urlopen. os.open sites untouched and still closed in a finally.
Task 32: minor (deferred): the hygiene scan keys on id(item.context_expr) only, so `with contextlib.closing(open(p))` is called bare (a managed handle) and a shadowed local def open(p) is flagged too; reach is top-level driver/*.py + template/*.py only, so bare json.load(open(...)) in the shell scripts' embedded python (create-board.sh:354,414; start-board.sh:73; reset.sh:83) is out of reach — "every file the engine opens" is true of the Python engine only (brief-scoped).
Task 32: minor (deferred): encoding widening is asymmetric — 5 readers pinned to UTF-8 while render-flow.py:197 still writes flow.mmd (non-ASCII em dashes) with the locale default and lanes.read_idea (template/lanes.py:489) still uses it; on an explicitly non-UTF-8 locale a write-then-check round trip could raise where it used to compare equal (PEP 538 coerces C/POSIX, so exotic).
Task 32: minor (deferred): doc-chain.py:222 read().splitlines() splits on more separators than `for line in f` (VT/FF/0x1c-1e/NEL/LS/PS), so such a raw line would fragment and be silently dropped — unreachable via the driver's own json.dumps writer.

Task 33 (The dict contracts get pinned; S10 + types S5/T-3/T-5/T-10/T-13/T-22): complete (stage-only, files: driver/run.py driver/runs_util.py template/board_schema.py template/card_render.py template/lanes.py tests/test_board_schema.py tests/test_chain_log.py tests/test_lanes_graph.py tests/test_render_body.py tests/test_rework_loop.py tests/test_runs_util.py; red: 6 failed/211 passed matching the brief verbatim (pinned-lane-card misclassification; DID NOT RAISE on max-reworks 0; header given twice unresolved; symlinked workdir said EXISTING PROJECT; AttributeError 'rework_key'; ValueError on malformed log_offset); one existing test REWRITTEN (REWRITTEN 2026-09-24, tests/test_board_schema.py::test_a_good_header_set_is_valid); task tests 217 passed; suite 806 = the brief's gate AND the plan's stated end state; render-flow --check exit 0; board_schema.py --check-schema current, exit 0; package 3451 lines; 45 files staged).
Task 33: reviewer found all 105 added lines present verbatim, no extra editing (mtime-proven), and MUTANT-VERIFIED the six contracts: reverting each producer (drop run id; old except ValueError block; old card_id_lane gate; if not set_to; abspath; delete the duplicate check) makes EXACTLY the intended test fail, and all six pass in isolation and both orders. No pinned contract contradicts an earlier task (rework_key reuses the mint run id that also names the run dir — run.py:164-171/3765; max_reworks' raise mirrors board_schema._kind_error("count") exactly; is_lane_card returns True for every LANE_CARDS title plus both round grammars and False for "Idea2:").
Task 33: minor (deferred): the report claims record_chain_starts' observed=True is pinned — it is not (the test's lambda defaults observed=False, and dropping observed=True still passes); the lane-int and gate ARE pinned.
Task 33: minor (deferred): rework_key fallback — use_run(None) sets STATE.run_dir = RUNS_ROOT, so basename -> "runs" and the pre-fix board-global shape b-runs-rev-P1-1 returns in the no-run state; the `or "no-run"` branch is unreachable via use_run.
Task 33: minor (deferred): named risk (a) resolved — the ValueError is unreachable through a validated door (a lane header max-reworks: 0 raises earlier in lanes.parse_idea (template/lanes.py:406-408); a board.json max-reworks: 0 is refused first by require_manifest_valid (run.py:3844, called :3949) with SystemExit before the lock). The only changed behaviour is a board.json edited WHILE the driver serves (the leniency require_manifest_valid's docstring declares at run.py:3853-3856): old code used the default 3, now ValueError per tick -> repeated-error halt. No fresh start is affected.
Task 33: minor (deferred): named risk (b) resolved — no regression for a legitimate boards-own workdir (abs, relative, trailing slash, ../b/work, symlinked board_dir all still PREVIOUS RUN; bind mounts unaffected since realpath resolves symlinks not mounts); the change is symlink-only and additionally corrects a symlink inside the board pointing outside.
Task 33: minor (deferred): ledger_log_offsets — a non-hashable card_id (JSON list) raises TypeError from setdefault outside the try (driver/runs_util.py:160), so the new except TypeError reads as if covered; unreachable for driver-written records.
Task 33: minor (deferred): validate_headers' "first on line N" is the previous occurrence for a 3rd repeat (brief-prescribed text). Tests name the producer module+symbol but no line numbers.

=== FINAL WHOLE-BRANCH REVIEW (after Task 33) ===
Ran `git diff -U10 HEAD` over the whole working tree (45 files, +2954/-461, 7777 lines) into
/home/wos/.hermes/profiles/coder/cache/scratch/final-review/whole-branch.diff, plus a consolidated
deferred-items brief (ledgered-deferred.md), and dispatched SIX read-only aspect reviewers in parallel:
A correctness/cross-task consistency, B silent failures, C tests + rewrite-marker audit, D prose truth,
E data shapes, F security. Full synthesis: final-review.md (this directory).
Verdicts: A/B/C/D/E/F = READY WITH DEFERRED ITEMS, NOT READY (shell-door registry read), READY, READY,
READY, READY. No Critical in any aspect. All five Review Focus bullets delivered (two probed end to end,
three read end to end); no task's change silently undone (six "misses" all legitimate supersessions);
Task 31's 14 broad catches in run.py verified as log/record/escalate.
Fix-now list (13 items) in final-review.md, incl.: run-id containment applied to 1 of 6 readers
(F/A/T15); reset.sh:159-190 registry read unchecked -> prints "not in the registry" and exits 0 (B);
doc-chain.py:100/:150 parse_ts unguarded -> traceback defeats bullet 2 (A); per-lane model/provider
advertised+pinned but cannot be honoured -> subprocess TypeError at the filing door (A/E/T13);
chain.jsonl readers need inputs dict / attached,staged lists (E); predictable .tmp without O_EXCL
(F); record_timing skip drops the card log (B/D); run.py:3439 per-tick provenance note (B);
run.py:2263 index WARNING once-per-condition + E2 trip (B, T31); reset.sh:151 staged read unchecked (B);
doc-chain history() uncounted verdicts drops (B); a 13-line prose cluster (D); T30's missing REWRITTEN
marker (C, benign — no test asserts markers).
Kept as deferred: T0/T2/T3/T7/T8/T9/T10/T12-test/T16/T18/T20/T21/T24/T27/T29/T31-inert/T32/T33 items,
plus pre-existing items (mode drift, acquire_lock flake, GIT_DIR scrub, --slug argv, card ids in paths,
PASS_THROUGH unused, --json key collision).
State: 45 files staged, NOTHING committed, suite 806 passed 0 skipped (env-stripped), render-flow --check 0,
board_schema --check-schema current. No stray processes; no repo file mutated by reviewers.

=== FIX PASS (after the final review) + COMMIT ===
User decision: run the fix pass now, then commit the 34-task work + fixes as ONE commit.
Wave 1 (3 units, disjoint files): unit 1 driver/run.py + tests (record_timing's skipped card log -> writes runs: null,
7 new tests; per-tick provenance note -> once per (run_dir, lane); the unbounded index WARNING -> once per
condition via STATE.index_read_failed + "(non-fatal)" marker, 0 E2 findings where it made 3; four predictable
.tmp writers -> one _write_atomic() with mkstemp at all four sites; four false prose sentences; the two missing
REWRITTEN 2026-09-24 markers). Unit 2 driver/file_lanes.py + doors (containment in unstarted_mint/next_run_key;
set_current_run via mkstemp; NEW public lanes.lane_value + file_board indexing per-lane model/provider arrays;
seven prose sentences incl. the schema $comment regenerated: --check-schema current). Unit 3 runs_util/run-audit/
runs-report/timing-report/reset.sh/doc-chain (containment in the four remaining readers; reset.sh registry read ->
exit 4 and staged read -> exit 1 instead of reporting a false fact and exiting 0; parse_ts never raises and a
bad/missing ts is skipped AND counted; inputs/attached/staged type tolerance; history() counts unreadable verdict
lines; timing-report's per-lane bullet made true).
Wave 2: unit 4 driver/run.py lane_board_cfg() + card_model_args + open_lane comparison -> per-lane arrays honoured
on the driver side too (3+2 new tests in test_manifest_shape/test_open_lane/test_gate_action).
Controller: removed probe litter (untracked boards/runs/cards/t_c.jsonl) and all __pycache__; patched
template/lanes.py:331-337 to name BOTH doors; ran the whole suite (855 passed).
Waves 3-4 (verification, read-only): verifier A (correctness/safety/regression) = CORRECT WITH FOLLOW-UPS;
verifier B (tests/claims) = HONEST WITH GAPS; both: all 13 fix-now items genuinely fixed, no weakened assertion
(only the two wording updates the code forced).
Unit 5 closed the five residuals: the duplicated test in tests/test_doc_chain.py (deleted the added copy);
runs/current mode back to 0666&~umask (WRITE_MODE, pinned by a test); reset.sh matches the slug against STDOUT
only and takes stdout only for the staged read; _write_atomic reads the umask ONCE at import (no per-write
toggle, pinned); a ts-less record is counted as unreadable. Suite 863 passed.
COMMITTED as 2116eca (46 files: 41 M + 5 A, +4442/-543). Worktree clean, no .pyc, no untracked litter.
Left open for a follow-up plan (residuals the verifiers ranked, deliberately not fixed): create-board.sh:466 still
folds stdout+stderr for its registry probe (same class as reset.sh, but it fails CLOSED — a false "already exists
- refusing"); run-audit's E3 prose under-enumerates the counted classes; an import-time umask ignores a later
change; runs-report.current_run returns None (not "") for an empty pointer; resolve_run_dir's lazy file_lanes
import is a new ImportError mode for out-of-tree callers; unpinned paths (answered-CLI enrichment, _write_atomic
mode parity before unit 5, ts-less records before unit 5).
