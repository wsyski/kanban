{
  "$comment": "Generated by template/board_schema.py --write-schema; that module is the authority. A manifest this schema ACCEPTS can still be refused there: a per-lane array must have exactly `lanes` entries, an `abspath` must exist on the host, a `duration` of zero means no budget at all, and a `provider` (or `provider_override`) needs a model beside it \u2014 one value per lane when the provider is per-lane. Checks JSON Schema cannot express.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "patternProperties": {
    "^\\$": {}
  },
  "properties": {
    "$schema": {
      "description": "Path to this generated schema.",
      "type": "string"
    },
    "assignees": {
      "additionalProperties": {
        "minLength": 1,
        "type": "string"
      },
      "default": {},
      "propertyNames": {
        "enum": [
          "coder",
          "human-gate",
          "researcher"
        ]
      },
      "type": "object"
    },
    "auto-gates": {
      "default": [],
      "items": {
        "enum": [
          "Gi",
          "Gp",
          "Gc"
        ]
      },
      "type": "array",
      "uniqueItems": true
    },
    "default-workdir": {
      "pattern": "^/",
      "type": "string"
    },
    "goal-cards": {
      "default": [],
      "items": {
        "enum": [
          "I",
          "P",
          "TW",
          "C",
          "TI"
        ]
      },
      "type": "array",
      "uniqueItems": true
    },
    "goal-max-turns": {
      "default": 40,
      "minimum": 1,
      "type": "integer"
    },
    "integration-tests": {
      "default": true,
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "lanes": {
      "default": 1,
      "minimum": 1,
      "type": "integer"
    },
    "max-retries": {
      "const": 1,
      "default": 1,
      "description": "a failed card is final \u2014 only a REVIEW sends work back, by filing a revision card"
    },
    "max-reworks": {
      "default": 3,
      "oneOf": [
        {
          "minimum": 1,
          "type": "integer"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "minimum": 1,
            "type": "integer"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "max-runtime": {
      "default": "60m",
      "pattern": "^\\s*(?:\\d+(?:\\.\\d+)?\\s*[hms]\\s*)+$",
      "type": "string"
    },
    "model": {
      "oneOf": [
        {
          "minLength": 1,
          "pattern": "\\S",
          "type": "string"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "minLength": 1,
            "pattern": "\\S",
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "model_override": {
      "minLength": 1,
      "pattern": "\\S",
      "type": "string"
    },
    "name": {
      "minLength": 1,
      "pattern": "\\S",
      "type": "string"
    },
    "provider": {
      "oneOf": [
        {
          "minLength": 1,
          "pattern": "\\S",
          "type": "string"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "minLength": 1,
            "pattern": "\\S",
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "provider_override": {
      "minLength": 1,
      "pattern": "\\S",
      "type": "string"
    },
    "refinement": {
      "default": true,
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "sequential": {
      "default": false,
      "type": "boolean"
    },
    "slug": {
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "type": "string"
    },
    "targets": {
      "default": [],
      "items": {
        "pattern": "^(/|~$|~/)",
        "type": "string"
      },
      "type": "array",
      "uniqueItems": true
    },
    "timeout-min": {
      "default": 240,
      "minimum": 1,
      "type": "integer"
    },
    "unit-tests": {
      "default": true,
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order \u2014 exactly `lanes` entries",
          "items": {
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    }
  },
  "title": "kanban board.json",
  "type": "object"
}
