VERDICT CARD — implementation review for lane <N>. Never edit or stage anything.

HARD RULES: (1) Read-only on the repository; write only `<RUNS>/scratch/<YOUR-CARD-ID>/review.md` (create the directory) — nothing temporary belongs in `work/`. The one exception is not yours to decide: running the plan's Run commands lays down the build's and the test runner's OWN output under <WORKDIR> (a `target/`, a cache, a virtualenv) — that is the tool's, it is not a deliverable, and it is never a reason to skip the run. Report it in the result if it lands where the plan says nothing should. (2) Do not commit, branch, stash, reset, restore or clean. (3) Do not write profile memories or create, patch or delete skills — the profile serves every later card.

<TOOLCHAIN_BOUNDARY>

TASK: review what this lane staged, against the plan at <PLAN> and the lane's contract at <REFINED> — the raw idea at <IDEA> only when <REFINED> says `refinement failed`. This lane's files are <REFINED>, <PLAN> and exactly the files the plan's Files blocks name, under <WORKDIR> or a declared target root (<TARGETS>); anything else in the index belongs to someone else and is not this lane's to judge. The TW and C cards worked BESIDE each other under the plan gate, so no card observed the tests failing before the implementation existed — (f) is where that is re-derived. Check:
(a) Every [C] step is implemented, and nothing beyond the plan is.
(b) The tests exercise the behaviour they claim to: run the suite YOURSELF, here, with the plan's Run commands — the build among them where the plan has one — because this is the first card to run them with both patches in the tree. On a lane with integration tests the suite at THIS card is the unit level: the integration tests do not exist yet, their card comes after you, and the final review runs the whole thing — so a missing integration suite is not a rejection here. Your verdict covers the tree as it stands at this card; where the integration card later changes code to make an end-to-end run reachable, the FINAL review re-derives the code checks on the tree the gate receives. A lane that runs no unit tests has no suite to run; the plan's Run commands and its `manual at Gc` criteria are then the evidence.
(c) Every file the plan names under <WORKDIR> is staged (`git -C <WORKDIR> diff --cached --name-only`); a file the plan names under a declared target root (<TARGETS>) is outside the repository, so verify it by path — it exists and holds what the plan names — and list it in the result. The patches attached to the TW and C cards touch nothing the plan does not name, and they touch no file IN COMMON: those two cards wrote this tree at the same time, so a file in both patches is a lost update the plan's Files blocks should have kept apart. No commits, no branches.
(d) Errors are not silently swallowed.
(e) Where the lane runs unit tests: the TW card's tests are unchanged by the C card — diff the staged test files against the patch attached to the TW<N> card — except a correction the C card attached as `test-fix.diff` and named in its result. Check every such change against the plan: the TW card was `done` before it, the C card's evidence shows the original assertion unsatisfiable, the replacement keeps the plan step's intent, no test is deleted and coverage does not shrink. A test change that fails any of these, or one the C card did not declare, is REJECT with `OWNER: C`. A test defect the C card named but did not correct (`TEST DEFECT (not corrected)`) is re-derived here; when it holds, it is REJECT with `OWNER: TW`.
(f) The tests can fail for the reason they claim. The plan states the FAIL it expects; read each new test's assertion against the patch the C card attached and say whether that test could have failed before this lane's change — a test that passes whatever the implementation does is not evidence. Where the plan says the behaviour was already there, say that instead: the criterion is then the PASS.
NO CHANGE IS A VALID OUTCOME: where the plan's smallest correct change is nothing, the lane passes with an empty staged set and the criterion re-derived by running it — say so in the result rather than requiring a diff. An unchanged tree is not an unfinished lane.
Reproduce, do not skim: every PASS is re-derived, every REJECT carries reproduction steps.

VERDICT — the result field's first word is the verdict:
- PASS: `hermes kanban --board <BOARD> complete <YOUR-CARD-ID> --result "PASS: (a)-(f) hold — build <ok>, unit <totals>"`
- REJECT: `hermes kanban --board <BOARD> complete <YOUR-CARD-ID> --result "REJECT: <numbered findings, each with reproduction steps> OWNER: <C|TW|TI>"` — the driver files ONE revision round (up to the board's rework cap, then a human) for the card that OWNS the fix, so name it: `OWNER: C` when the implementation is wrong, `OWNER: TW` when a unit test is wrong, `OWNER: TI` when an integration test is wrong. The C card corrects a TW test only under the conditions check (e) names, so a test defect it did not correct belongs to the TW card: the round goes to the card you name, and a verdict with no usable owner line goes to the C card.
Write the full review to `<RUNS>/scratch/<YOUR-CARD-ID>/review.md` — the driver attaches it when your card is done, and you never attach anything yourself. If you cannot complete every check, the verdict is `REJECT: incomplete review — <what is unchecked>`, never a PASS with caveats.

<RESULT_FIELD>

<WORKER_CONTRACT>
