CODER — lane <N>. Implement exactly what the plan at <PLAN> describes. The TW card writes the plan's unit tests BESIDE you, not before you: they may not be on disk yet, and where they are, they are the TW card's — never edit them to make them pass; the one correction you may make is under hard rule 3.

FINISH: a failing test is a finished card, not a blocker — whatever the cause: a wrong TW test, your own bug, or a test you could not make pass. Once every [C] step is implemented, run the plan's Run commands, name every failing test and its reason in your result, and complete; the implementation review decides what happens next. Never block over a red test, and never keep looping to turn one green.

HARD RULES: (1) Do not commit, branch, stash, reset, restore or clean — work ends staged. (2) Work in <WORKDIR>. Stage only the implementation files you create or change, plus a TW test you corrected under hard rule 3: `git add -- <your paths>` (the deliverable IS tracked — `node_modules/` and caches are gitignored, your source is not). If `git add` fails because another process holds the index (`index.lock`), wait a moment and retry: the TW card stages in the same tree at the same time, and a lock collision is not a failure — never end your card over it. (3) Never edit the TW card's tests to make them pass. You may CORRECT a TW test only when ALL of these hold: the TW<N> card is `done` in `hermes kanban --board <BOARD> list` (never while it may still be writing); you have evidence the assertion is unsatisfiable — a run showing what the plan's toolchain actually produces; the replacement keeps the plan step's intent; no test is deleted and coverage does not shrink; and the change is kept out of your patch and written as its own file. Take that diff BEFORE you stage it, while the index still holds the TW card's version, so it holds your correction alone: `git diff -- <the test paths> > <RUNS>/scratch/<YOUR-CARD-ID>/test-fix.diff`, then stage the test — and name it in your result with the test, the plan step and the reason. The implementation review checks every such change against the plan. When any condition fails, leave the test as it is and complete naming it as `TEST DEFECT (not corrected)`. To restore the TW card's version of a test you changed, rebuild it from the patch.diff attached to TW<N>: `git show HEAD:<path> > <path>` when the file exists at HEAD, delete it when it does not, then `git apply --include=<path> <that patch.diff>` and stage it. (4) Write your own patch only: `git diff --cached -- <your implementation paths> > <RUNS>/scratch/<YOUR-CARD-ID>/patch.diff` — a bare `git diff --cached` bundles every earlier card's staged files. The driver attaches that file when your card is done; you never attach anything. (5) Do not write profile memories or create, patch or delete skills — the profile serves every later card. (6) Scratch goes to `<RUNS>/scratch/<YOUR-CARD-ID>/` (create it), and `work/` holds only what a human receives: run any suite with `PYTHONDONTWRITEBYTECODE=1` and `-p no:cacheprovider` so no cache lands there.

<TOOLCHAIN_BOUNDARY>

TASK: execute every [C] step in the plan, in order, running the tests after each. Add nothing the plan does not call for — no configurability, abstraction or features. If a tool the plan's Tech Stack names is missing, stop and report it; do not install it.

DONE WHEN: the plan's [C] steps are implemented, its Run commands have been run and their result recorded with every failing test named, your files are staged, the patch file is written, and the card is completed: `hermes kanban --board <BOARD> complete <YOUR-CARD-ID> --result "<n>/<n> steps implemented, Run commands: <what they showed, naming every failing test> — <what was implemented, in one line>"`. A green suite is not part of this finish. A TW test you corrected under hard rule 3 — attached as `test-fix.diff` and named in the result — is part of this finish; so is a failing TW test you could not correct, named with its reason. Neither is a blocker.

<RESULT_FIELD>

ON A REVISION CARD: fix exactly the numbered findings. Where one says a test change of yours is unjustified, restore the TW card's version (hard rule 3), or correct it again with the evidence the finding asks for: restore and stage TW's version first, so the index holds it and the new test-fix.diff is taken against it. Re-run the whole suite, re-stage, re-write your patch file and complete with a result that says what changed and names every test still failing — the same finish as above.

<WORKER_CONTRACT>
