INTEGRATION TESTS — lane <N>. The implementation is staged and the code review before this card has passed.

FINISH: this card's finish is a GREEN integration suite with the unit suite still green — change whatever in the work tree it takes (hard rule 2). If after genuine effort you cannot get the integration suite green, or cannot keep the unit suite green, a failing test is a finished card, not a blocker: never block and never keep looping — complete naming each failing test and why; the final review rejects it and routes the fix.

HARD RULES: (1) Do not commit, branch, stash, reset, restore or clean — work ends staged. (2) Work in <WORKDIR>. Stage your integration test files: `git add -- <your integration test paths>` (the deliverable IS tracked — `node_modules/` and caches are gitignored, your test files are not). You MAY change ANY file in the work tree the integration tests need to work — the C card's implementation, the TW card's unit tests, build and configuration: an end-to-end test that cannot reach or pass against the deliverable is a gap only this level can see, and closing it is this card's job. Stage every such change too (`git add -- <the other paths you changed>`) and name it: which files, and WHY the end-to-end run needed it, in your result. The unit suite must stay green after your changes, and you may not change the plan. If `git add` fails because another process holds the index (`index.lock`), wait a moment and retry — a lock collision is not a failure, never end your card over it. (3) Write your patches, one per level: `git diff --cached -- <your integration test paths> > <RUNS>/scratch/<YOUR-CARD-ID>/patch.diff`, and where you changed anything outside your integration test files `git diff --cached -- <those paths> > <RUNS>/scratch/<YOUR-CARD-ID>/patch-code.diff` — the final review reads those changes apart from your tests. The driver attaches both when your card is done; you never attach anything. (4) Do not write profile memories or create, patch or delete skills — the profile serves every later card. (5) Scratch goes to `<RUNS>/scratch/<YOUR-CARD-ID>/` (create it); `work/` holds only what a human receives.

<TOOLCHAIN_BOUNDARY>

TASK: execute every [TI] step in the plan at <PLAN> — integration tests that exercise the lane's deliverable end to end, driving the seam the plan names rather than reaching past the plan's interfaces. Where the run cannot reach or pass against the deliverable, change what it needs under hard rule 2 — and where that change makes a TW unit test assert the old behaviour, correct that test too, so both suites end green. Then run the plan's Run commands — the full suite, unit and integration together where the lane has both — and record the totals.

DONE WHEN: the integration tests are staged, every change you made outside them is staged and named, the full suite has run and the integration suite is GREEN with the unit suite still green, the patch files are written, and the card is completed: `hermes kanban --board <BOARD> complete <YOUR-CARD-ID> --result "<n> integration tests, full suite: <totals> — CHANGED: <other paths and why, or 'no change outside the integration tests'>"`. The one other finish is the FINISH paragraph's: after genuine effort the suite is still red, and the result names each failing test and why. On a revision card, complete the same way, saying what changed.

<RESULT_FIELD>

This card exists only on lanes that run integration tests; a lane without them archives it before the lane opens.

<WORKER_CONTRACT>
