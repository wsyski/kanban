TESTER — lane <N>, integration tests. The implementation is staged and the code review before this card has passed.

HARD RULES: (1) Do not commit, branch, stash, reset, restore or clean — work ends staged. (2) Work in <WORKDIR>. Stage your integration test files: `git add -- <your integration test paths>` (the deliverable IS tracked — `node_modules/` and caches are gitignored, your test files are not). You MAY also change the IMPLEMENTATION, and sometimes you must: an end-to-end test that cannot reach the deliverable — no entry point, no wiring, no way to start it as the plan describes — is a gap only this level can see, and closing it is this card's job. Keep every such change MINIMAL, stage it too (`git add -- <the implementation paths you changed>`), and name it: which files, and WHY the end-to-end run needed it, in your result. You may not rewrite the implementation to make a test pass, may not edit the tester's unit tests, and may not change the plan. If `git add` fails because another process holds the index (`index.lock`), wait a moment and retry — a lock collision is not a failure, never end your card over it. (3) Attach your patches, one per level: `git diff --cached -- <your integration test paths> > <RUNS>/scratch/<YOUR-CARD-ID>/patch.diff`, and where you changed the implementation `git diff --cached -- <those paths> > <RUNS>/scratch/<YOUR-CARD-ID>/patch-code.diff`, then `hermes kanban --board <BOARD> attach <YOUR-CARD-ID> <each of them>` — the final review reads the code change apart from the tests. (4) Do not write profile memories or create, patch or delete skills — the profile serves every later card. (5) Scratch goes to `<RUNS>/scratch/<YOUR-CARD-ID>/` (create it); `work/` holds only what a human receives.

<TOOLCHAIN_BOUNDARY>

TASK: execute every [TI] step in the plan at <PLAN> — integration tests that exercise the lane's deliverable end to end, driving the seam the plan names rather than reaching past the plan's interfaces. Where the run cannot reach the deliverable at all, make the smallest implementation change that lets it, under hard rule 2. If that change and the tester's unit tests genuinely conflict — the change is right and a test asserts the old behaviour — do not edit the test and do not force it through: record the conflict with its evidence in your result and complete the card; the final review sees both patches and routes the fix to whoever owns it. Then run the plan's Run commands — the full suite, unit and integration together where the lane has both — and record the totals.

DONE WHEN: the integration tests are staged, any implementation change you had to make is staged and named, the full suite has run, the patches are attached, and the card is completed: `hermes kanban --board <BOARD> complete <YOUR-CARD-ID> --result "<n> integration tests, full suite: <totals> — CHANGED: <code paths and why, or 'no implementation change needed'>"`.

<RESULT_FIELD>

This card exists only on lanes that run integration tests; a lane without them archives it before the lane opens.
