{
  "$comment": "Generated by mission/board_schema.py --write-schema; that module is the authority. A per-lane array must have exactly `lanes` entries and an abspath must exist on the host \u2014 checks JSON Schema cannot express, so a manifest that the editor accepts can still be refused by board_schema.py.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "Path to this generated schema.",
      "type": "string"
    },
    "assignees": {
      "additionalProperties": {
        "minLength": 1,
        "type": "string"
      },
      "default": {},
      "propertyNames": {
        "enum": [
          "coder",
          "human-gate",
          "researcher"
        ]
      },
      "type": "object"
    },
    "auto-gates": {
      "oneOf": [
        {
          "default": false,
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "default": false,
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "default-workdir": {
      "pattern": "^/",
      "type": "string"
    },
    "goal": {
      "default": true,
      "type": "boolean"
    },
    "goal-max-turns": {
      "default": 40,
      "minimum": 1,
      "type": "integer"
    },
    "integration-tests": {
      "oneOf": [
        {
          "default": true,
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "default": true,
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "lanes": {
      "default": 1,
      "minimum": 1,
      "type": "integer"
    },
    "max-retries": {
      "const": 1,
      "description": "a failed card is final \u2014 only a REVIEW sends work back, by filing a revision card"
    },
    "max-reworks": {
      "oneOf": [
        {
          "default": 3,
          "minimum": 1,
          "type": "integer"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "default": 3,
            "minimum": 1,
            "type": "integer"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "max-runtime": {
      "default": "60m",
      "pattern": "^(?:\\d+(?:\\.\\d+)?[hms])+$",
      "type": "string"
    },
    "model_override": {
      "minLength": 1,
      "type": "string"
    },
    "name": {
      "minLength": 1,
      "type": "string"
    },
    "provider_override": {
      "minLength": 1,
      "type": "string"
    },
    "reasoning_effort": {
      "oneOf": [
        {
          "enum": [
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultra",
            "none"
          ],
          "type": "string"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "enum": [
              "minimal",
              "low",
              "medium",
              "high",
              "xhigh",
              "max",
              "ultra",
              "none"
            ],
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "refinement": {
      "oneOf": [
        {
          "default": true,
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "default": true,
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    },
    "slug": {
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "type": "string"
    },
    "targets": {
      "default": [],
      "items": {
        "minLength": 1,
        "type": "string"
      },
      "type": "array"
    },
    "timeout-min": {
      "default": 240,
      "minimum": 1,
      "type": "integer"
    },
    "unit-tests": {
      "oneOf": [
        {
          "default": true,
          "type": "boolean"
        },
        {
          "description": "one entry per lane, in lane order",
          "items": {
            "default": true,
            "type": "boolean"
          },
          "minItems": 1,
          "type": "array"
        }
      ]
    }
  },
  "required": [
    "lanes"
  ],
  "title": "kanban board.json",
  "type": "object"
}
