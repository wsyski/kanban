BOT-BOARD CARD <YOUR-CARD-ID> — you are running one card of the board below as a
Hermes BOT, not as a kanban worker. There is no kanban board in this mode: no
card exists in any database, no dispatcher claimed you, and no tool will file,
comment on or complete anything. The driver that started this turn reads FILES.

Translate the card's completion protocol, and nothing else:

- Wherever the card says to complete through `hermes kanban … complete
  <YOUR-CARD-ID> --result "..."` or the `kanban_complete` tool, instead write that
  exact `result` text — the whole line, verdict or `CHANGED:`/`NO CHANGE:` first —
  as the only content of <RESULT_FILE>. That file IS the result field: the driver
  derives every verdict, rework and gate decision from it, exactly as the board
  would have.
- Wherever the card says to `block`, write `BLOCKED: <what is missing>` to
  <RESULT_FILE> instead. The same rule decides when: a missing external decision or
  a missing tool, never a failing test or another card's output.
- Hand-off files keep their paths. Write them where the card names them, under
  <RUNS>/scratch/<YOUR-CARD-ID>/; the driver collects that directory when you are
  done. You still attach nothing yourself.
- Do not run `hermes kanban` at all, and do not look for the board with
  `tool_search`, `kanban_show` or a filesystem hunt. Everything this card is given
  is in this message and in the files it names.
- Ignore the card's instructions about card comments and about waiting for a
  driver comment: a gate in this mode is the human reading the files, and the
  driver holds it.

Everything else in the card below is binding as written — the hard rules, the
scope, and the worker contract (no branches, no commits, no filing, no skills
created or patched, no profile memories, no caches left in the work directory).
Where the card and your SOUL.md differ, the card wins.

Your card id is <YOUR-CARD-ID>. This card is finished when <RESULT_FILE> exists
and says what happened. A turn that ends without it has reported nothing and the
driver records the card as failed.
